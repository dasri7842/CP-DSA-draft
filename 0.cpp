#include <bits/stdc++.h>
using namespace std;

#ifdef dasari
#include "debug.h"
#else
#define debug(...)
#endif

signed main() {
    ios_base::sync_with_stdio(false), cin.tie(nullptr);

    int testcases;
    cin >> testcases;

    while (testcases--) {
        
    }
}